export class Tabla {

  beca: string;
  beneficio: string;
  descripcion: string;
  requisitos: string;

  constructor(beca: string, beneficio: string, descripcion: string, requisitos: string) {
    this.beca = beca;
    this.beneficio = beneficio;
    this.descripcion = descripcion;
    this.requisitos = requisitos;
  }
}
